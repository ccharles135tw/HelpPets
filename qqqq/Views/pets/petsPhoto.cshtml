
@model IEnumerable<qqqq.ViewModels.CProductShow>
<section class="section-margin--small mb-5">

    <!-- End Filter Bar -->
    <!-- Start Best Seller -->
    <section class="lattest-product-area pb-40 category-list">
        <div class="row">

            @foreach (var p in Model)
            {
                <div class="col-md-10 col-lg-4">
                    <div class="card text-center card-product">
                        <div class="card-product__img">
                            <img width="300" height="300" src="/Images/@p.Photos.FirstOrDefault()?.PictureName" alt="">
                            <ul class="card-product__imgOverlay">
                                <li>
                                    <button id="addToList" style="background:#EAA636;">
                                        <i class="fa-solid fa-list-alt">@Html.ActionLink("詳細資料", "petsDetial", "pets")</i>
                                    </button>
                                </li>
                            </ul>
                        </div>
                        <div class="card-body">
                            <p>品種:@p.SubCategoryName</p>
                            <h4 class="card-product__title"><a href="#">@p.ProductName</a></h4>
                        </div>
                    </div>
                </div>  
                }
        </div>
    </section>
    <!-- End Best Seller -->

</section>
@section scripts{

}